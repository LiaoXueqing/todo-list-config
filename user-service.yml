server:
  port: 8081
spring:
  jpa:
    hibernate:
      ddl-auto: none
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user?autoReconnect=true&useSSL=false
    username: root
    password: password
